# Your PERSONAL LiteLLM API key — each student gets their own ($25 budget).
# Do NOT share it and do NOT commit it (.env is gitignored).
# opencode reads this via {env:LITELLM_API_KEY} in opencode.json.
LITELLM_API_KEY=sk-your-own-key-here
